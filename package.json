{
  "name": "traverse-fs",
  "version": "0.0.5",
  "description": "Nodejs npm module to traverse folder using code or cli or use glob patterns traverse-cli or traverse-fs or fssys",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "preinstall" : "", 
    "install" : "", 
    "postinstall" : "", 
    "install:cli": "npm install -g traverse-cli --force",
    "test": "mocha --reporter spec --recursive --config ./mocha.test.config.js"
  },
  "bin": {
    "traverse": "./src/cli.js",
    "fsutils": "./src/cli.js",
    "fst": "./src/cli.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ganeshkbhat/glob-traverse-fs.git"
  },
  "keywords": [
    "traverse",
    "traverse files",
    "traverse folders",
    "search",
    "search files",
    "search folders",
    "traverse-cli, traverse-fs, fssys"
  ],
  "author": "Ganesh Bhat",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/ganeshkbhat/glob-traverse-fs/issues"
  },
  "homepage": "https://github.com/ganeshkbhat/glob-traverse-fs#readme",
  "devDependencies": {
    "chai": "^4.3.6",
    "mocha": "^10.0.0",
    "sinon": "^14.0.0"
  },
  "dependencies": {
    
  }
}
