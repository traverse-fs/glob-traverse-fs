import { TraverseResult } from './traverse';
import { Context } from './index';
export interface ProcessedResult {
    unresolved: string[];
    unimported: string[];
    unused: string[];
    clean: boolean;
}
export declare function processResults(files: string[], traverseResult: TraverseResult, context: Context): Promise<ProcessedResult>;
